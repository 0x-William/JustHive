import { StyleSheet } from 'react-native';
import { WHITE } from 'AppColors';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center'
  },
  margin: {
    marginTop: 5,
  },
  icon: {
    top: -20,
    width: 18.8,
    height: 15.6,
    tintColor: WHITE,
  },
});
