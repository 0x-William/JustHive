import { StyleSheet } from 'react-native';
import { BG_GRAY_NORMAL } from 'AppColors';

export const styles = StyleSheet.create({
  container: {},
  title: {
    textAlign: 'center',
    marginBottom: 10,
    fontWeight: 'bold',
    fontSize: 10
  },
  socialList: {
    backgroundColor: BG_GRAY_NORMAL
  }
});
