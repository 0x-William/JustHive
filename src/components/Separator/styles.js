import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 15,
    marginBottom: 15,
    backgroundColor: '#e6e6e6'
  },
  separator: {
    height: 3
  }
});
